# test.py 可调参数文档

## 一、顶层全局常量（目前硬编码）

| 变量 | 位置 | 当前值 | 含义 |
|------|------|--------|------|
| `MODEL_PATH_VLM` | L36 | `"./vlm"` | Phase1 使用的 VLM 模型路径 |
| `MODEL_PATH_LLM` | L37 | `"./llm"` | LLM 模型路径（当前未使用） |
| `REPORT_OUTPUT_DIR` | L38 | `"./reports_lira_lite"` | 所有输出报告和图表的保存目录 |
| `RANDOM_SEED` | L39 | `42` | 全局随机种子，控制可复现性 |

---

## 二、DynamicsToolkit.extract_features() — 物理特征提取

| 参数 | 行号 | 当前值 | 含义 |
|------|------|--------|------|
| `head_end_idx` 比例 | L66 | `0.5`（前 50%） | 计算 descent rate 截取 loss 序列前多少比例 |

> ⚠️ 注释写的是"前 30%"，但代码实际用的是 `0.5`（前 50%）。如想改为前 30%，把 `int(T * 0.5)` 改为 `int(T * 0.3)`。

| `tail_start_idx` 比例 | L82 | `0.5`（后 50%） | 计算 tail fluctuation 从序列的哪里开始取尾部 |

---

## 三、Phase1Screener — VLM 筛选阶段

| 参数 | 位置 | 含义 |
|------|------|------|
| `self.prompt` 文本 | L117–L124 | 发给 VLM 的提示词，描述了 Member/Non-member 的视觉特征定义 |
| `torch_dtype` | L110 | `"auto"` — 加载 VLM 时的精度，可改为 `torch.float16` 降低显存 |
| `device_map` | L110 | `"auto"` — 多卡分配策略 |

---

## 四、Phase2Investigator — 物理特征校准阶段

| 参数 | 行号 | 当前值 | 含义 |
|------|------|--------|------|
| `score_phy` 融合权重 | L206 | `0.5` / `0.5` | `p_roc` 和 `p_acc` 在物理分数中的权重比 |
| `final_score` 融合公式 | L209 | `alpha * score_phy + 1.0 * vlm_score` | VLM 分数系数固定为 `1.0`，物理分数系数为自适应 `alpha` |
| `eps` | L200–L201 | `1e-5` | 防止 p_roc/p_acc 为 0 或 1 时的数值裁剪边界 |

---

## 五、LiraLiteAuditor.run() — 主流程核心超参

| 参数 | 行号 | 当前值 | 含义 |
|------|------|--------|------|
| **校准池伪负样本阈值** | L262 | `< 0.1` | VLM score 低于此值视为高置信伪负样本，进入校准池 |
| **校准池最小样本数** | L264 | `10` | 校准池样本数低于此值时回退 |
| **回退比例** | L266 | `bottom 10%` | 回退时取 VLM score 最低的 10% 作为校准池 |
| **alpha 权重上限** | L279 | `5.0` | `adaptive_alpha` 的封顶值，防止物理分数占比过大 |
| **Phase2 跳过阈值** | L291 | `< 0.1` | VLM score 低于此值直接信任 VLM，跳过 Phase2 |

---

## 六、LiraLiteAuditor.load_data() — 数据加载

| 参数 | 当前值 | 含义 |
|------|--------|------|
| `max_samples` | `600`（默认值） | 总测试样本数，member/non-member 各半 |
| `random.shuffle` | 开启 | 加载后打乱样本顺序 |

---

## 七、报告/图表生成相关

| 参数 | 行号 | 当前值 | 含义 |
|------|------|--------|------|
| `dpi` | 多处 | `300` | 所有保存图表的 DPI 分辨率 |
| `bins` | L337, L359 | `50` | 直方图的分箱数 |
| `shared_bins` | L322, L342 | `30` | 对比直方图的共享分箱数 |
| `figsize` | L319 | `(18, 5)` | 假设验证图尺寸 |
| FPR 阈值线 | L359 | `99.9%`（Top 0.1% FPR） | 最终分数分布图中的决策阈值 |

---

## 八、命令行入口参数（通过 run_attack 传入）

| 参数 | 默认值 | 含义 |
|------|--------|------|
| `--dataset` | `CIFAR100` | 数据集名 |
| `--model` | `mobilenet` | 模型名 |
| `--max_samples` | `1000` | 最大测试样本数 |

---

## 九、调参优先级建议

如果要进行消融实验，推荐按以下优先级调整：

1. **`max_samples`** — 样本量直接影响结果稳定性
2. **校准池阈值 `< 0.1`** — 决定哪些样本用于建立背景分布
3. **`DynamicsToolkit` 的 `0.5` 比例** — 头部/尾部切分点，探索不同切分对特征区分力的影响
4. **`score_phy` 的 `0.5/0.5`** — rate_of_change vs tail_fluctuation 的权重
5. **alpha 上限 `5.0`** — 控制物理分数最高能放大多大倍数
